# 仓库协作约束

## 公共协作原则

- 本文件只记录开源贡献者和自动化代理都应遵守的最小项目规则，不承载维护者个人开发习惯、私有计划或敏感上下文。
- 不提交密钥、令牌、本机绝对路径、私有模型资产或运行日志。
- 新增实现优先复用项目既有模块和 helper，避免重复造轮子；新增抽象必须能减少真实复杂度或降低耦合。
- 代码注释只解释复杂边界、设计原因或风险点，不添加空泛重复注释。
- 公开文档以简体中文维护；代码标识符、依赖包名、接口路径、配置键、协议字段和第三方专有名称按既有命名约定保留。

## 产品阶段与平台范围

- Muse 当前工程版本为 `v1.0.0-beta.2`。Beta 表示核心主链已经形成可持续使用基线，后续重点是项目认知、真实缺陷审计、架构收口和体验修正；不表示已经完成公开发布准备。
- macOS 是当前主要开发和真实桌面验证环境，但不代表已经达到公开发布质量，也不构成发布日期承诺。
- 从 `v1.0.0-beta.2` 源码标签起支持 Windows 11 25H2 x64（build 26200）与 Evergreen WebView2 111+，固定实机为 Windows 11 Pro；支持声明只有同一提交和同一 NSIS 测试包完成全部门禁后才生效。当前不提供公开 Windows 安装包，Windows 10、Windows 11 24H2/26H1、ARM64 和 Windows Server 不在支持范围。
- 允许从 `dev` 为确认可用的内部 Beta 基线创建形如 `vX.Y.Z-beta.N` 的预发布标签；预发布标签只运行 quality，不触发安装包或 GitHub Release。未经维护者以后明确启动正式发布评估，不创建稳定版标签、公开安装包或 GitHub Release。

## 分支与版本流转契约

- `master` 是稳定主分支，也是未来正式发布的唯一可能来源；稳定版标签只能来自 `master`。
- `dev` 是长期开发集成分支，是日常推送和 Pull Request 的默认目标。`dev` 可以创建明确带预发布后缀的内部 Beta 标签，也可以通过手动工作流构建短期测试安装包；两者都不得自动创建正式 GitHub Release。
- 除紧急 `hotfix` 外，功能、修复、重构、样式、文档和测试改动都从最新 `dev` 创建单一目标的临时分支，不得直接合入 `master`。
- 临时分支合入前必须 rebase 到最新 `dev`；多个相互依赖的临时分支应按依赖顺序收口到一个临时集成分支。重新通过质量门禁后，优先使用快进方式合入 `dev`。
- `dev` 的测试版本通过自动化门禁和真实环境验收后，才允许提升到 `master`。共享的 `dev` 与 `master` 禁止强制推送或直接 rebase；不能快进时必须先查明分叉原因。
- 临时分支完成集成并确认无需回滚取证后应删除。紧急 `hotfix` 合入 `master` 后必须同步回 `dev`，不得形成只存在于发布分支的长期修复。

## CI 与发布契约

- `.github/workflows/ci.yml` 是自动化执行的唯一事实来源；内部 Beta 标签只运行 quality。双平台安装包与 Release 路径只允许未来不含预发布后缀、来自 `master` 的稳定版标签触发，不能把 Beta 标签当作安装包或发布授权。
- quality 门禁不得降级或跳过，至少包含 Rust 格式、严格 Clippy、Rust 全量测试、本地 API 安全契约、前端 typecheck、Vitest、生产构建、包体和内嵌 UI 契约检查。
- GitHub Actions 必须固定到完整提交 SHA，默认 `GITHUB_TOKEN` 权限保持只读；只有创建 Release 的 job 可以声明 `contents: write`。CI 不使用真实供应商密钥，不在日志、artifact 或就绪文件中写入 token。
- 普通提交和 Pull Request 不得触发安装包或 Release。修改 workflow、权限、runner、缓存、artifact 或未来发布条件时，必须验证 fork PR 安全边界，并确保不会扩大秘密访问或发布权限。
- 未来启动版本阶段评估时，必须先根据当时批准的平台范围重写安装包、签名、公证、资产和校验和契约；不得沿用当前遗留的双平台资产要求推断发布范围。
- CI 失败应先读取失败 job 和日志、修复根因并运行对应本地门禁；不得通过降低安全断言、删除测试、放宽 `-D warnings` 或反复无诊断重跑来制造绿色结果。
